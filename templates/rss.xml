<?xml version="1.0" encoding="UTF-8"?>
{% set feed_pages = pages %}
{% if section %}
  {% for subsection_path in section.subsections %}
    {% set subsection = get_section(path=subsection_path) %}
    {% if not subsection.transparent %}
      {% set_global feed_pages = feed_pages | concat(with=subsection.pages) %}
    {% endif %}
  {% endfor %}
{% endif %}
{% set feed_pages = feed_pages | sort(attribute="date") | reverse %}
<rss xmlns:atom="http://www.w3.org/2005/Atom" version="2.0">
  <channel>
    <title>{{ config.title }}{% if section and section.title %} - {{ section.title }}{% endif %}</title>
    <link>{% if section %}{{ section.permalink | escape_xml | safe }}{% else %}{{ config.base_url | escape_xml | safe }}{% endif %}</link>
    <description>{% if section and section.description %}{{ section.description }}{% else %}{{ config.description }}{% endif %}</description>
    <generator>Zola</generator>
    <language>{{ lang }}</language>
    <atom:link href="{{ feed_url | safe }}" rel="self" type="application/rss+xml"/>
    {% if feed_pages | length > 0 %}<lastBuildDate>{{ feed_pages[0].date | date(format="%a, %d %b %Y %H:%M:%S %z") }}</lastBuildDate>{% endif %}
    {%- for page in feed_pages %}
    <item>
      <title>{{ page.title }}</title>
      <pubDate>{{ page.date | date(format="%a, %d %b %Y %H:%M:%S %z") }}</pubDate>
      <author>{% if page.authors %}{{ page.authors[0] }}{% elif config.author %}{{ config.author }}{% else %}Unknown{% endif %}</author>
      <link>{{ page.permalink | escape_xml | safe }}</link>
      <guid>{{ page.permalink | escape_xml | safe }}</guid>
      <description xml:base="{{ page.permalink | escape_xml | safe }}">{% if page.summary %}{{ page.summary }}{% else %}{{ page.content }}{% endif %}</description>
    </item>
    {%- endfor %}
  </channel>
</rss>
